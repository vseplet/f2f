[project]
name = "f2f-mac-ui-tests"
version = "0.0.0"
description = "HTTP-level integration tests for f2f-mac ui"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pytest>=7.0",
]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
testpaths = ["."]
