{
  "name": "f2f-camp",
  "version": "0.1.0",
  "description": "Rendezvous server for f2f peers — identity, rooms, and address exchange.",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3"
  }
}
